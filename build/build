#!/bin/bash
cd ..
name=$1
if [ -f manifest-$1.json ]
then
	cp manifest-$1.json manifest.json
	zip -r build/ANTC-$name.zip . -x "build/*" ".git/*" "manifest-chrome.json" "manifest-firefox.json" "*.zip" "README.md" ".vscode/*"
	rm manifest.json
else
	echo "No existe ese manifest: "$name
fi